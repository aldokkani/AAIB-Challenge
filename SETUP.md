## Running the Application

#### Building the React.js app artifacts
`$ cd ./client`  
`$ npm ci`  
`$ cp .env.sample .env`  
`$ npm run build`

#### Running the server

`$ cd ./server`  
`$ npm ci`   
`$ npm start`

#### Then navigate to `localhost:8000/`